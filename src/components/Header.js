import React, { Component, PropTypes } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                <h1>Config here</h1>
            </header>
        );
    }
}

Header.propTypes = {

};

export default Header;